coverage:
  precision: 2
  round: down
  range: 60..85
  status:
    project:
      default:
        target: auto
        threshold: 2%
    patch:
      default:
        target: 60%
        threshold: 5%

comment:
  layout: "reach,diff,flags,tree"
  behavior: default
  require_changes: false
